# Instagram export + downloaded media + login session — never commit these
saved_posts.json
saved_posts.html
saved_collections.json
saved_collections.html
downloads/
*.session
.downloaded.txt
failures.log
__pycache__/
*.pyc
